# ======================================
# הגדרות סביבה למערכת SaaS
# ======================================
# העתק קובץ זה ל-.env וערוך את הערכים

# Django Core Settings
# ======================================
SECRET_KEY=your-secret-key-here-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings
# ======================================
# אפשרות 1: SQLite (פיתוח)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# אפשרות 2: PostgreSQL (production)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=inventory_db
# DATABASE_USER=inventory_user
# DATABASE_PASSWORD=your_strong_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# אפשרות 3: Database URL (Heroku/Cloud)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Security Settings
# ======================================
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Email Settings
# ======================================
# אופציה 1: Console (פיתוח - מדפיס לקונסול)
# #EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# אופציה 2: Gmail SMTP (מומלץ - חינם עד 500 מיילים ביום)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=shaharprod@gmail.com
EMAIL_HOST_PASSWORD=qqsq orjo ppww raph
DEFAULT_FROM_EMAIL=shaharprod@gmail.com

# אופציה 3: SendGrid (חינם עד 100 מיילים ביום)
# #EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=shaharprod@gmail.com
# EMAIL_HOST_PASSWORD=   .\setup_email.ps1
# DEFAULT_FROM_EMAIL=shaharprod@gmail.com

# אופציה 4: Mailgun
# #EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=shaharprod@gmail.com
# EMAIL_HOST_PASSWORD=   .\setup_email.ps1
# DEFAULT_FROM_EMAIL=shaharprod@gmail.com

# Storage Settings (אופציונלי)
# ======================================
# AWS S3 Storage
# USE_S3=False
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1

# Redis Cache (אופציונלי)
# ======================================
USE_REDIS=False
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
# או השתמש ב-URL מלא:
# REDIS_URL=redis://localhost:6379/0
# REDIS_URL=redis://:password@localhost:6379/0

# Sentry Monitoring (אופציונלי)
# ======================================
# SENTRY_DSN=your-sentry-dsn-here

# API Settings
# ======================================
API_RATE_LIMIT=100/hour
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Multi-Tenancy Settings
# ======================================
DEFAULT_TENANT_PLAN=basic
MAX_TENANTS_PER_USER=5

# Security Settings
# ======================================
RATE_LIMIT_PER_MINUTE=100
SESSION_CHECK_IP=False
SESSION_CHECK_USER_AGENT=False
